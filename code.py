# Launches the Ships shell
#
# This is done in a sperate file so that __name__ is set to "Ships" instead of "__main__"
# which helps some modules detect that they are running in Ships.

import kahless
